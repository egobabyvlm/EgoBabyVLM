# @group multimodal
# DevBench Gram-TROG Evaluation

defaults:
  - devbench
  - _self_

output_dir: "${oc.env:HOME}/egobabyvlm_experiments/evals"

_target_: "evaluation.multimodal.devbench.base.DevBenchGramTROGEvalModule"

name: "gram-trog"

dataset:
  _target_: "evaluation.data.devbench.DevBenchDataset"
  name: "gram-trog"
  kwargs:
    dataset_folder: "${oc.env:DEVBENCH_DATA_ROOT}/gram-trog"

human_data_path: "${oc.env:DEVBENCH_DATA_ROOT}/gram-trog/human.csv"

model: ${model}

seed: 42
