# ContrastiveFeatureExtractor wrapping a trained-from-scratch (or converted-from-legacy)
# contrastive checkpoint. Override `kwargs.checkpoint_path` for your run.
_target_: "apps.baselines.clip.trained_extractor.ContrastiveFeatureExtractor"
name: "contrastive"
kwargs:
  checkpoint_path: ???
  normalize: true
