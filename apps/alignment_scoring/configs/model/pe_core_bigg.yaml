# Perception Encoder Core bigG-14 at 448px resolution (Meta-released weights).
# Destination decided by the consumer's `defaults` line.

model_name: PE-Core-bigG-14-448
pretrained: meta
mode: eval
is_video_model: false
