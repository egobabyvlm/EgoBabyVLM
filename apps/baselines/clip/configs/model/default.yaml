# Default multimodal model: shared embedding dim + temperature, plus references
# to the text and vision encoder groups (set via the top-level config.yaml).
# @package model

embedding_dim: 512
normalize_features: true
temperature: 0.07
fix_temperature: false
